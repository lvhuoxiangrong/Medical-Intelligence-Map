# 医智图前端设计 · JSON 数据使用说明

> 本文档回答两个问题：**HTML 页面里怎么用这两份 JSON**，以及**以后怎么改数据、加数据**。
> 如果你是第一次运行，先看第 2 节「怎么打开页面」。

---

## 1. 文件夹里有什么

```
医智图前端设计/
├── index.html            系统总览页（首页，设计方案展示）
├── doctor.html           医生端演示页（决策辅助对话，论据已改为动态读取 JSON）
├── 数据浏览.html         数据浏览器（核心演示页：把两份 JSON 全部渲染出来）
├── 启动页面.bat          一键启动本地服务器（双击它，浏览器自动打开）
├── server.ps1            备用服务器脚本（bat 的兜底方案，无需 Python）
├── 使用说明.md           本文档
└── json文件/             两份 JSON 数据源
    ├── guideline_frames.json              指南论据框架（27 条，共 339 KB）
    └── diabetes_comments_dataset.json     糖尿病论坛讨论数据集（12 个主题、77 条评论，49 KB）
```

---

## 2. 怎么打开页面（重要！）

**不要直接双击 HTML 文件**。浏览器有安全限制（file:// 协议下网页不允许读取本地 JSON），直接双击打开时，页面会提示「JSON 加载失败」。

正确的打开方式（二选一）：

### 方式 A：双击「启动页面.bat」（推荐，什么都不用管）
1. 双击 `启动页面.bat`
2. 它会**自动检测**电脑上的可用启动方式：`py` 启动器 → 真实 python → 内置 PowerShell 服务器（无需 Python），挑一个能用的启动
3. 等待 2 秒，浏览器自动打开 `http://localhost:8000/index.html`
4. 想停止服务器：关掉那个黑色命令行窗口即可

### 方式 B：手动命令行
```bash
# 先进入文件夹
cd C:\Users\17632\Desktop\医智图前端设计
# 再启动服务器 —— 注意用 py，不要用 python！
py -m http.server 8000
```
然后浏览器访问 `http://localhost:8000/数据浏览.html`。

> 只要是通过 `http://localhost:8000/...` 访问的，JSON 就能正常加载。
> `数据浏览.html` 页面顶部有实时状态标签，一眼就能看出两份 JSON 加载成功没有。

---

## 3. 为什么「python -m http.server 8000」会报错？（你的截图）

你截图上那个报错 `Python was not found; run without arguments to install from the Microsoft Store...`
是 Windows 常见的坑，原因一句话说清：

> **你电脑上其实已经装了真正的 Python 3.14**（在 `C:\Users\17632\AppData\Local\Programs\Python\Python314\`），
> 但这个目录**没有加进系统 PATH**。于是命令行里敲 `python` 时，系统找到的是微软商店的"占位程序"
> （在 `C:\Users\17632\AppData\Local\Microsoft\WindowsApps\` 下），它不会运行真正的 Python，
> 只会提示你去商店安装。

**解决办法（三选一）：**

| 办法 | 说明 |
| --- | --- |
| ✅ 用 `py` 代替 `python` | `py` 是 Python 官方启动器（你机器上已存在），它能自动找到已安装的 Python 3.14。`启动页面.bat` 现在就是优先用 `py` |
| ✅ 直接用「启动页面.bat」 | bat 已做自动检测：py → 真 python → 纯 PowerShell 服务器，三种都能用 |
| 🔧 把 Python 目录加进 PATH | 一劳永逸，以后 `python` 命令也能直接用，见下面「可选：修复 PATH」 |

### 可选：修复 PATH（让 python 命令永久可用）
1. 按 `Win + R`，输入 `sysdm.cpl` 回车，点「高级」→「环境变量」
2. 在「用户变量」里选中 `Path` → 编辑 → 新建，粘贴这两行：
   ```
   C:\Users\17632\AppData\Local\Programs\Python\Python314\
   C:\Users\17632\AppData\Local\Programs\Python\Python314\Scripts\
   ```
3. 确定保存，**重开一个命令行窗口**，输入 `python --version` 验证

---

## 4. 两份 JSON 长什么样（数据结构）

### 4.1 guideline_frames.json —— 指南论据框架

最外层是一个**数组**，数组里每一项是一条「指南论据框架」（本数据集共 27 条，编号 gf-0 ~ gf-26）。

每条框架的关键字段：

| 字段 | 含义 | 示例 |
| --- | --- | --- |
| `clinical_scenario` | 临床场景（英文枚举） | `glycemic_target` |
| `scenario_detail` | 场景的中文详细描述 | 住院期间个人持续葡萄糖监测（CGM）设备的持续使用… |
| `target_population` | 适用人群（疾病/并发症/场景/用药状态） | `{disease:["T1DM","T2DM"], care_setting:"inpatient_icu", …}` |
| `trigger_conditions` | 触发条件（summary 总述 + items 明细） | `{summary:"住院的糖尿病患者…", items:[…]}` |
| `claim` | 中文结论摘要（一句话推荐意见） | 建议住院糖尿病患者…继续使用个人CGM设备 |
| `evidence_strength` | 证据强度（level 推荐级别 + grade 等级） | `{level:"recommended", grade:"B", sources:[…]}` |
| `caveats` | 注意事项 / 适用边界（数组） | `[{content:"必须确保资源和培训到位…", severity:"moderate"}]` |
| `rationale` | 理由 / 依据（数组） | `[{role:"clinical_experience", content:"…"}]` |
| `original_text` | 指南原文（英文摘录） | `16.6 In people with diabetes using a personal CGM…` |
| `quality` | 抽取质量自检 | `{overall:"medium", checks:[…]}` |

> 小技巧：`doctor.html` 里论据详情侧栏显示的「gf-0 ~ gf-26」，现在就是按数组顺序从这份 JSON 动态生成的，一一对应。

### 4.2 diabetes_comments_dataset.json —— 患者论坛讨论

最外层是一个**对象**，包含 `metadata`（数据说明）和 `threads`（主题数组）。

```json
{
  "metadata": { "total_threads": 12, "total_comments": 85, "clinical_scenarios": [...], ... },
  "threads": [
    {
      "id": "SC-001",
      "source_platform": "百度贴吧",
      "clinical_scenario": "胰岛素起始治疗的心理抗拒",
      "author": "控糖小白2025",
      "title": "医生让我打胰岛素我害怕啊求安慰",
      "main_post": "各位糖友好，本人男45岁……",
      "replies": [
        { "author": "糖友老兵张哥", "content": "楼主别怕，我打胰岛素打了六年了……" },
        { "author": "内分泌小医生", "content": "从医学角度说几句……" }
      ]
    }
  ]
}
```

| 字段 | 含义 |
| --- | --- |
| `id` | 主题编号（SC-001 起） |
| `source_platform` | 来源平台（百度贴吧 / 知乎） |
| `clinical_scenario` | 讨论涉及的临床场景（中文） |
| `author` | 楼主昵称 |
| `title` | 帖子标题 |
| `main_post` | 主帖正文 |
| `replies` | 回复数组，每项 `{author, content}` |

> ⚠️ 注意：这份 JSON 的 `metadata.total_comments` 写的是 85，但实际统计 `replies` 只有 **77 条**（12 个主题分别是 7+6+8+6+7+6+6+6+6+7+6+6）。页面上的数字以实际统计为准（77）。如果你想保持元数据一致，可以把 metadata 里的 85 改成 77，或补足评论。

---

## 5. HTML 是怎么用这些 JSON 的

核心就三步，`数据浏览.html` 里全部实现了，可以对照源码看。

### 第 1 步：fetch 读取（异步）
```js
fetch("json文件/guideline_frames.json")
  .then(function(r){ return r.json(); })   // 把 JSON 文本解析成 JS 对象
  .then(function(data){
    // data 就是可直接使用的 JS 数组/对象
  });
```

### 第 2 步：把数据存进一个全局对象
```js
var STATE = { frames: [], threads: [] };
// 加载后：STATE.frames = 27 条论据；STATE.threads = 12 个主题
```

### 第 3 步：写渲染函数，生成 HTML 卡片
```js
function renderFrames(){
  var html = STATE.frames.map(function(f, i){
    return '<div class="gf-card">' + f.claim + '</div>';
  }).join("");
  document.getElementById("gridFrames").innerHTML = html;
}
```

### doctor.html 是怎么改的
原来论据详情（gf-0 ~ gf-26）是**写死在 HTML 里的 245 行硬编码数据**，现在改成了：

1. 页面加载时执行 `loadGuidelinesFromJson()`，用 `fetch("json文件/guideline_frames.json")` 读取；
2. 每一条 JSON 框架被转换成原来同格式的 `GUIDE_EV["gf-N"]` 对象（结论、证据强度、来源、注意事项、原文快照）；
3. 示例问答里引用 `evId:"gf-17"` 这类编号的论据，点击后自动命中动态数据，流程完全不变。

页面顶栏会实时显示「指南论据 N 条 · 已从 JSON 加载」，加载失败会出现红色提示条。

---

## 6. 怎么改数据、加数据

**原则：只改 JSON，不动 HTML，刷新页面就生效。**

### 加一条新的指南论据
1. 用记事本/VS Code 打开 `json文件/guideline_frames.json`；
2. 在数组末尾（最后一个 `}` 后面）加逗号，再复制粘贴一条现有框架，改掉 `clinical_scenario`、`scenario_detail`、`claim`、`evidence_strength`、`original_text` 等字段；
3. 保存 → 刷新页面。它会自动变成 `gf-27`，各页面统计数字也会自动 +1。

> 注意 JSON 语法：键和字符串必须用双引号 `"`，最后一项后面不能有多余逗号。改完如果不确定对不对，可以用 https://www.json.cn 之类的在线工具校验。

### 加一个新论坛主题
同样方式，在 `diabetes_comments_dataset.json` 的 `threads` 数组末尾加一个对象，注意 `id` 不要重复（下一个是 SC-013）。

### 改数字/文案
直接改 JSON 里的值即可。所有页面统计（论据条数、评论条数、图表）都是加载时**实时计算**的，不需要改 HTML。

---

## 7. 常见问题（FAQ）

| 问题 | 解决 |
| --- | --- |
| 提示 `Python was not found...`（微软商店占位程序） | 不要用 `python` 命令，改用 `py -m http.server 8000`，或直接双击「启动页面.bat」（会自动选可用的方式） |
| 双击 bat 后浏览器没打开 | 等 2 秒手动访问 `http://localhost:8000/index.html`；如果提示无法访问，看黑色窗口里的报错文字 |
| 改了 JSON，刷新页面没变化 | 浏览器缓存了旧文件。按 `Ctrl+Shift+R` 强制刷新，或关掉服务器重开 |
| 页面提示 JSON 加载失败 | 你是直接双击 HTML 打开的。改用「启动页面.bat」或 `py -m http.server 8000` |
| 想查看加载有没有报错 | 按 `F12` 打开开发者工具 → Console（控制台），红色报错会显示具体原因 |
| 想用回 `python` 命令 | 见第 3 节「可选：修复 PATH」 |

---

## 8. 三个页面各自负责什么

| 页面 | 用途 | 是否读 JSON |
| --- | --- | --- |
| `index.html` | 系统总览 / 设计方案展示 | 否（纯展示页） |
| `doctor.html` | 医生端决策辅助演示（对话 + 论据详情） | 是（guideline_frames.json → 论据侧栏） |
| `数据浏览.html` | 把两份 JSON 全部渲染：筛选、搜索、图表 | 是（两份都读） |

从 `数据浏览.html` 能直接跳到另外两个页面（顶部导航），`doctor.html` 顶部也有「数据浏览」入口。
